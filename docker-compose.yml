version: '3.8'

services:
  telegram_bot:
    build: .
    container_name: taxi_broadcast_bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data.json:/app/data.json
      - ./sessions:/app/sessions
